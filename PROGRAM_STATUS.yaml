# Единственный источник фазы, версии и гейтов. Меняется чаще всех — потому отдельный файл.
program: agentarium
version: 0.1.0        # MAJOR — пивот точки Б · MINOR — принятая итерация «спека+код» · PATCH — фикс кода без изменения бумаги
phase: build            # бумага принята владельцем, идёт имплементация по spec/70-roadmap.md

gates:
  G0-paper-accepted:
    status: closed_accepted   # бумага принята владельцем 2026-07-19 после 4 раундов кросс-модельного ревью + финального сеньор-взгляда
    seal: 0c5561170463b8701b18db271b3f025a0ef964df
  G1-core-built:       # слайсы S1-S6 закрыты
    status: closed_accepted   # ядро построено; live-smoke чата GigaChat прошёл зелёным 2026-07-19
  G2-poc-live:         # конфигурация A доказана живьём; живучесть доказана тестами+наблюдением. Статусы: pending|open|closed_accepted|skipped
    status: closed_accepted
  G3-release:          # чистый клон собирается (uv sync+ruff+110 unit+LICENSE ✓); полный make demo на клоне ждёт восстановления квоты GigaChat
    status: open

slices:
  S1-skeleton-envelope: done   # 10 контрактных тестов, ruff чист, схема экспортирована
  S2-bus-sdk: done   # SDK-эталон + инфра: 9 интеграционных тестов на живом брокере, 10 юнитов
  S3-topology: done   # рубеж 1 целиком, apply с реконсиляцией и дренажом, генератор, echo→reverse по чертежу
  S4-observability: done   # водопад подтверждён живым Jaeger API
  S5-storage: done   # 54 unit + 21 integration; паспорт id=0, инжест идемпотентен; хост-порт pg → 5433
  S6-gateway: done   # шлюз универсален, образ собран, 62 unit + 27 integration; e2e echo-цепочка зелёная
  S7-poc-demo-resilience: done  # конфигурация A доказана живьём (model routing Lite+Ultra, реальные проверки БД); живучесть — 37 integration + наблюдение в бою; наглядный chaos.sh ждёт стабильного pipeline (не блок)
  S8-extended-config: code_done_live_pending  # auditor + dba-extended: 102u+36i зелёные; живой прогон ждёт ключ
  S9-polish-release: in_progress  # витрина+LICENSE+CI-ярус готовы, находки ревью репо закрыты, чистый клон собирается; полный e2e-демо клона и chaos-запись ждут квоту GigaChat

authorized:
  implementation: true    # открыто гейтом G0
  github_push: true
  publish_repo: false     # публичным репо становится только перед сдачей
